# Src

Código fuente del sitio. Se genera y evoluciona trabajando con Claude Code sobre este repo, usando `content/`, `design/` y `.claude/skills/` como input.

Esta carpeta queda vacía hasta que definas el framework (Next.js, Astro, etc.) — Claude Code puede hacer el scaffold inicial una vez tengas `content/` y `design/tokens.json` poblados con contenido real.
